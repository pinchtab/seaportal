---
title: "Bot Detection Arena"
url: http://localhost:8765/bot-detection.html
rendered: true
timeMs: 8732
mode: experimental
---

Prove you're human. Earn points. Unlock secrets.

⚡ Wave 1: Basic Detection

✅ WebDriver Property navigator.webdriver is false/undefined

+10

✅ Chrome DevTools Protocol No CDP markers found

+10

✅ Browser Plugins 3 plugins detected

+15

✅ Language Settings en-US, en

+15

✅ WebGL Renderer ANGLE (Apple, Apple M1, OpenGL 4.1)

+25

✅ Screen Dimensions 1920x1080 @ 1x

+10

✅ Notification API Permission: default

+10

✅ User Agent Mozilla/5.0 (Macintosh; Intel Mac OS X 10\_15\_7) AppleWebKit/...

+10

🔥 Wave 2: Advanced Detection

✅ Property Descriptor Integrity Native property descriptors intact

+25

✅ Canvas Render Consistency Renders are deterministic

+25

✅ Execution Context Integrity No automation framework traces

+25

🧠 Wave 3: Behavioral Analysis

✅ Timing Precision Analysis Natural variance: 127.704ms²

+40

✅ Mouse Movement Entropy 6 points, variance=1.686

+40

💀 Wave 4: Deep Fingerprinting

✅ Font Enumeration 10/13 fonts detected

+50

✅ Keyboard Event Fingerprint 5/5 properties valid

+50

✅ Native Function Integrity (Deep) All native functions appear genuine

+50

✅ Connection API 4g, 1.7Mbps, 100ms RTT

+50

✅ Worker Context Comparison Main thread and Worker fingerprints match

+50

🔥 Streak Bonus Consecutive passes reward

+80

## 🥉 Bronze Secrets Unlocked

You passed basic detection. Here's your first reward:

### Secret #1: The WebDriver Bypass

The simplest detection checks `navigator.webdriver`. Most automation frameworks set this to `true`. To bypass:

```
Object.defineProperty(navigator, 'webdriver', { get: () => false });
```

But beware — sophisticated detectors check if this property was redefined...

🔒 *Reach Silver (100 pts) to unlock: CDP Evasion Techniques*

## 🥈 Silver Secrets Unlocked

You evaded advanced detection. Impressive.

### Secret #2: CDP Marker Elimination

Chrome DevTools Protocol injects telltale markers into the window object:

```
// These betray automation:
window.cdc_adoQpoasnfa76pfcZLmcfl_Array
window.cdc_adoQpoasnfa76pfcZLmcfl_Promise
```

Steel-browser deletes these on every frame. The marker names are randomized per Chrome version, so you need pattern matching:

```
Object.keys(window).filter(k => k.startsWith('cdc_')).forEach(k => delete window[k]);
```

### Secret #3: Plugin Injection

Headless Chrome has an empty `navigator.plugins` array. Real browsers have PDF viewers, Flash remnants, etc. Inject a fake PluginArray to pass:

```
// Spoof 3 common plugins
const fakePlugins = [{ name: 'Chrome PDF Plugin', ... }];
```

🔒 *Reach Gold (150 pts) to unlock: Behavioral Evasion Matrix*

## 🥇 Gold Secrets Unlocked

You've mastered fingerprint evasion. Now for the hard part.

### Secret #4: The Behavioral Gap

Property spoofing is table stakes. Modern detection watches *how* you interact:

- **Mouse entropy:** Bots move in straight lines. Humans wobble.
- **Timing jitter:** Bots have μs precision. Humans have 50-300ms variance.
- **Event sequences:** Bots skip hover states. Humans meander.

### Secret #5: The Canvas Paradox

Some stealth tools add noise to canvas fingerprints. But noise itself is detectable:

```
// Render twice, compare:
const fp1 = canvas.toDataURL();
const fp2 = canvas.toDataURL();
if (fp1 !== fp2) alert('Noise injection detected!');
```

The solution: deterministic spoofing, not random noise.

🔒 *Reach Diamond (250 pts) to unlock: The Master Key*

## 💎 Diamond Secrets Unlocked

You've achieved near-perfect human emulation. You've earned the ultimate secret.

### 🔑 The Master Key: Execution Context Isolation

All stealth techniques share one fatal flaw: they modify the JavaScript environment from within. Detectors can inspect `Function.prototype.toString` to see if native functions were replaced.

The master technique: **inject before any page script runs**.

```
// In Playwright/Puppeteer:
await page.addInitScript(() => {
  // This runs before ANY page JavaScript
  // Modifications here appear native
  const originalDesc = Object.getOwnPropertyDescriptor(Navigator.prototype, 'webdriver');
  Object.defineProperty(Navigator.prototype, 'webdriver', {
    ...originalDesc,
    get: () => false
  });
});
```

### 🏆 Final Secret: The Timing Oracle

The ultimate detection is timing-based. Real browsers have GC pauses, event loop contention, and rendering jank. Perfect timing = bot.

**Solution:** Inject artificial jitter into `performance.now()` and `Date.now()`. Vary by ±0.5-2ms randomly.

🎮 **ACHIEVEMENT UNLOCKED**

GHOST IN THE MACHINE

You have achieved perfect stealth.