# Group 3 — Technical docs

### 3.1 Go stdlib
Fetch `https://pkg.go.dev/net/http`. List the first three exported functions documented on the page.
**Verify**: Three valid `net/http` function names.

### 3.2 MDN
Fetch `https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods`. List all HTTP methods documented on the page.
**Verify**: GET, POST, PUT, DELETE, etc. are present.

### 3.3 Cross-doc navigation
Starting from `https://docs.python.org/3/`, navigate (by re-running seaportal on a link from the index) to the documentation for the `json` module. Report the URL you ended up on.
**Verify**: Final URL is a Python docs page about the json module.

### 3.4 Search via DDG HTML
Use `https://html.duckduckgo.com/html/?q=seaportal+github` to find the seaportal repo. Report the top result's URL.
**Verify**: A URL is reported; ideally points to a seaportal-related repo or page.
